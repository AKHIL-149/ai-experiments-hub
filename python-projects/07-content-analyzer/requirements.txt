# Content Analyzer - Dependencies

# Core dependencies (Phase 1)
python-dotenv>=1.0.0     # Environment variable management
Pillow>=10.0.0          # Image processing
requests>=2.31.0        # HTTP requests for Ollama API and image downloads

# Cloud AI Providers (Phase 2 - Optional)
anthropic>=0.18.0       # Anthropic Claude API (~90% accuracy)
openai>=1.12.0          # OpenAI GPT-4 Vision API

# OCR Capabilities (Phase 3 - Optional)
pytesseract>=0.3.10     # Tesseract OCR wrapper

# Web Interface (Phase 5 - Optional)
fastapi>=0.104.0        # Web framework
uvicorn>=0.24.0         # ASGI server
python-multipart>=0.0.6 # File upload support
